<div class="col-md-4 col-md-offset-4">
  <% if @group.errors.any? %>
	<ul>
		<% @group.errors.full_messages.each do |msg| %>
		  <li> <%= msg %></li>
		<%end%>
	</ul>
  <% end %>
  <h2>Create new group</h2>
  <hr>
  <%= render "form" %>
</div>
